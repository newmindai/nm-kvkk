[build-system]
requires = ["setuptools>=77,<82"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["gliner2", "gliner2.*"]

[tool.setuptools.package-data]
gliner2 = ["py.typed"]

[tool.setuptools.dynamic]
version = {attr = "gliner2.__version__"}

[project]
# Distribution name differs from upstream so this modified copy is not mistaken for the PyPI package `gliner2`.
# The import name stays `gliner2`.
name = "gliner2-nm-kvkk"
description = "Modified copy of fastino-ai/GLiNER2 used by nm-kvkk (native tokenization, bf16 loss fix)"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.10"

authors = [
    {name = "Fastino AI (upstream GLiNER2)"},
]
maintainers = [
    {name = "NewMind AI (this modified copy)"},
]

classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: OS Independent",
]

dependencies = [
    "pydantic>=2.0,<3",
    "requests>=2.28,<3",
    "tqdm>=4.64,<5",
    "urllib3>=1.26,<3",
]

dynamic = ["version"]

[project.urls]
Repository = "https://github.com/newmindai/nm-kvkk"
Upstream = "https://github.com/fastino-ai/GLiNER2"
Changes = "https://github.com/newmindai/nm-kvkk/blob/main/GLiNER2/CHANGES.md"

[project.optional-dependencies]
local = [
    "numpy>=1.24,<3",
    "peft>=0.10,<1",
    "safetensors>=0.4,<1",
    "torch>=2.1,<3",
    "transformers>=4.38,<5",
]
train = [
    "numpy>=1.24,<3",
    "peft>=0.10,<1",
    "PyYAML>=6,<7",
    "safetensors>=0.4,<1",
    "torch>=2.1,<3",
    "transformers>=4.38,<5",
]
test = [
    "packaging>=24.2,<27",
    "pytest>=8,<9",
    "pytest-cov>=5,<8",
    "tomli>=2,<3; python_version < '3.11'",
]
dev = [
    "build>=1,<2",
    "mypy>=1.10,<2",
    "numpy>=1.24,<3",
    "packaging>=24.2,<27",
    "peft>=0.10,<1",
    "pytest>=8,<9",
    "pytest-cov>=5,<8",
    "PyYAML>=6,<7",
    "ruff>=0.6,<1",
    "safetensors>=0.4,<1",
    "scipy>=1.10,<2",
    "torch>=2.1,<3",
    "tomli>=2,<3; python_version < '3.11'",
    "transformers>=4.38,<5",
    "twine>=5,<7",
    "types-PyYAML>=6,<7",
    "types-requests>=2.28,<3",
]
benchmark = [
    "datasets>=3.0,<5",
    "numpy>=1.24,<3",
    "peft>=0.10,<1",
    "safetensors>=0.4,<1",
    "scikit-learn>=1.3,<2",
    "scipy>=1.10,<2",
    "torch>=2.1,<3",
    "transformers>=4.38,<5",
]

[tool.pytest.ini_options]
testpaths = ["tests", "packaging_tests"]
pythonpath = ["."]
markers = [
    "slow: end-to-end tests that load models or are otherwise slow",
    "quality: checkpoint-dependent inference quality expectations",
    "compile: torch.compile / export-path tests (platform dependent)",
]

[tool.ruff]
target-version = "py310"
line-length = 100
include = [
    "gliner2/__init__.py",
    "gliner2/training/__init__.py",
    "packaging_tests/**/*.py",
]

[tool.ruff.lint]
select = ["B", "E4", "E7", "E9", "F", "I", "UP"]

[tool.mypy]
python_version = "3.10"
files = [
    "gliner2/__init__.py",
    "gliner2/training/__init__.py",
]
incremental = true
cache_dir = ".mypy_cache"
check_untyped_defs = true
ignore_missing_imports = true
follow_imports = "silent"
warn_unused_configs = true

[tool.coverage.run]
branch = true
source = ["gliner2"]

[tool.coverage.report]
fail_under = 60
show_missing = true
skip_covered = true
exclude_also = [
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
