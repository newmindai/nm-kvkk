# Copy to .env (gitignored). Every entry is optional.

# Hugging Face token: only needed if a model or dataset you download is gated/private.
HF_TOKEN=

# kvkk_synth (synthetic data generation) — OpenRouter key for the writer/judge models. Costs money; see kvkk_synth/README.md.
OPENROUTER_API_KEY=
