Write a Turkish document of the type "{{ genre }}" (domain: {{ domain }}).
What this document type contains: {{ description }}
Format: {{ document_format }} — "structured" = a form/record-like layout with field labels and values; "unstructured" = free-flowing prose. Plain text only: no markdown symbols.

This document contains NO personal data at all: no person names, no identity, tax, account, card, phone, e-mail, address, plate, birth date, health, membership or belief information about anyone, and no named individuals. It may and should contain the ordinary non-personal content such a document carries: dates, amounts, quantities, product or service names, law and article numbers, room or order counts, institution and department names (no persons), generic roles ("the applicant", "the customer"). Do not use square brackets anywhere. Output ONLY the document text, and end with a final line containing exactly: ===SON===
