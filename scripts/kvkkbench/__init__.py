"""Cross-model KVKK entity benchmark: one pipeline (sentencizer -> per-unit prediction -> document offsets ->
taxonomy ids -> prediction files -> scoring) for GLiNER2 checkpoints and open token-classification baselines.
See README.md (Benchmarks)."""
